# Used for debugging
debug_mode=false

# Whether to use symbols or a numerical value in the score display
display_symbols=true

# What characters the output shows.
game_characters=⬛,🟨,🟩 # A more colorful approach to the ouput
# game_characters=-,?,X # For accessability of consoles which cannot print emojis

# Whether to seed from today's date, or pick a random word each attempt.
seed_from_date=false